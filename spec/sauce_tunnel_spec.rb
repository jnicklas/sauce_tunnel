require "spec_helper"

describe SauceTunnel do
  it "has a version number" do
    expect(SauceTunnel::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
